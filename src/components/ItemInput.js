import React from 'react'

// class component that has a local state that holds the core values

class ItemInput extends React.Component {
    render() {
        return (
            <div> 
                Item Input Form for a new Item
            </div>
        )
    }
}

export default ItemInput